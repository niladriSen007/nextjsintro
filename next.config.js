/** @type {import('next').NextConfig} */
const nextConfig = {
    images:{
        domains:[
            //for external images
            "images.unsplash.com",
        ]
    }
}

module.exports = nextConfig
